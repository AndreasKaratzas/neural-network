/**
 * Progressbar.h
 *
 * In this header file, we define a
 * function that implements a basic
 * progress bar interface from scratch.
 */

#pragma once

#include "Common.h"

void printProgress(long double percentage, int width, char* overlay, char* message);
